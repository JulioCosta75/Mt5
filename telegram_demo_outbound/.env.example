# Copy this file locally if needed. Do NOT put real secrets here.
# TELEGRAM_BOT_TOKEN must be a Cursor Cloud Runtime Secret, never a committed value.
# TELEGRAM_CHAT_ID may be a Runtime Secret; otherwise it is discovered via getUpdates.

APP_MODE=demo
TELEGRAM_TRANSPORT=mock
